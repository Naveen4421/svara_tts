langcodes
language_data  # required by langcodes' Language.display_name()
snac
soundfile  # decodes reference audio; torchaudio 2.10 needs TorchCodec for that
# Install these from https://download.pytorch.org/whl/cu128, not PyPI.
# Held at 2.10.0: torch 2.11+ defaults to CUDA 13, which needs driver r580+.
torch==2.10.0
torchaudio==2.10.0
transformers>=4.56.0,!=5.0.*,!=5.1.*,!=5.2.*,!=5.3.*,!=5.4.*,!=5.5.0
vllm==0.19.1  # newest release whose wheel is a CUDA 12.8 build
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
aiohttp>=3.9.0
flashinfer-python>=0.2.3
requests
hf-transfer  # Optional: faster HuggingFace downloads